# 智能邮件处理系统 – Agent需求定义文档

| 项目     | 内容                                                         |
| :------- | :----------------------------------------------------------- |
| 文档版本 | v1.0                                                         |
| 目标用户 | 总经理及其授权助理                                           |
| 核心价值 | 每日节省1.5–2小时邮件处理时间，不错过关键信息，保持回复风格一致 |
| 安全红线 | **AI绝不自动发送邮件，所有外发内容必须经人工确认后才可发出** |

------

## 1. 业务场景与核心任务

### 1.1 典型的一天

总经理每天收到60–120封邮件，来源包括：

- 内部团队汇报、审批请求
- 客户、合作伙伴的商务沟通
- 供应商、服务商的日常往来
- 行业资讯、Newsletter、系统通知
- 垃圾邮件/钓鱼邮件

目前问题：

- 关键邮件被淹没，平均响应延迟超4小时
- 60%的邮件不需要总经理亲自处理，但缺乏可靠过滤
- 每次出差回来积压数百封邮件，处理压力巨大
- 助理代为筛选时，因不了解上下文而误判优先级

### 1.2 核心任务定义

| 任务         | 描述                                       | 输出                       |
| :----------- | :----------------------------------------- | :------------------------- |
| **邮件拉取** | 定时/按需从指定邮箱拉取未读邮件            | 结构化邮件对象             |
| **智能分类** | 按紧急度+类型+是否需要回复，打标签         | 分类标签+置信度            |
| **摘要生成** | 将长邮件/邮件群组对话压缩为3句话摘要       | 中文摘要文本               |
| **回复草拟** | 对需要回复的邮件，按总经理风格生成回复草稿 | 草稿文本+风格匹配度        |
| **每日简报** | 每天早上生成一份“今日需关注”邮件简报       | 结构化简报（按优先级排序） |

------

## 2. 系统架构选型

### 2.1 架构模式：**Router + Tool-use Loop**

**理由**：

- 邮件处理是典型的多步骤任务（拉取→分类→摘要→草拟），但每步决策逻辑明确
- Router负责根据用户意图分发（“帮我整理收件箱” vs “查看某封邮件详情” vs “给XX回信”）
- Tool-use Loop处理需要多步推理的场景（如“找出上周王总发来的合同邮件并草拟回复”）
- 此模式面向**用户交互与按需查询**；每日批量邮件处理走独立的规则优先流水线（见2.3），避免逐封循环LLM的开销

### 2.2 模块组成

```
用户交互层（Chat界面 / 定时任务）
        ↓
Agent 核心（Router + LLM推理）
        ↓
┌───────┼───────┬─────────┬──────────┐
↓       ↓        ↓         ↓          ↓
邮件    分类     摘要      回复草拟    安全
拉取    模型     引擎      引擎       网关
模块    模块     模块      模块       模块
```

### 2.3 邮件处理流水线（规则优先 + 批量LLM）

为同时满足「单封成本 <¥0.1」和「分类准确率≥90%」，每日批量处理采用**三级流水线**，LLM只处理模糊地带：

**第一级：规则粗筛（免费，覆盖全部邮件）**

- 发件人白名单/黑名单、域名信誉、关键词、正则规则
- 直接判定：`可忽略`（Newsletter/系统通知/广告）、`紧急`（命中告警/投诉关键词）、`可信发件人`
- 规则可解释、可离线调试、不消耗Token

**第二级：批量LLM分类（仅粗筛未定的邮件）**

- 剩余邮件按批次（约20封/次）一次性喂给模型，批量打「紧急度 + 动作」标签
- 输出带置信度；**置信度低于阈值（如0.7）的邮件自动进入人工复核队列**，不直接归入「可忽略」
- 批量调用比逐封循环节省80%以上分类成本

**第三级：深度处理（按需，仅进入工作流的目标邮件）**

- 只有需要「摘要 / 草拟 / 转交建议」的邮件才进行单封处理
- 摘要、草拟复用第3章定义的工具

**成本估算（示意，需按实际模型价格校准）**

以日均80封为例：第一级规则免费消化约40–55封；第二级约2次批量分类调用；第三级约20–30封摘要、10–15封草拟。日均LLM成本≈¥0.4–0.5，摊到全部邮件**单封均值<¥0.01**，远低于¥0.1/封预算，留有缓存与重试空间。

> 说明：2.1的 Router + Tool-use Loop 面向**用户交互与按需查询**（点开某封、搜索、指定回复），本流水线面向**每日后台批量处理**。二者共用第3章的工具层；流水线作为后台任务运行，不阻塞交互。

------

## 3. 工具定义

### 3.1 工具清单

| 工具名称           | 类型           | 描述                                               | 权限       |
| :----------------- | :------------- | :------------------------------------------------- | :--------- |
| `fetch_emails`     | 读             | 拉取指定范围的邮件（未读/指定日期/来自特定发件人） | 正常       |
| `get_email_detail` | 读             | 获取单封邮件的完整内容（含附件名称）               | 正常       |
| `search_emails`    | 读             | 按关键词、发件人、日期范围搜索邮件                 | 正常       |
| `classify_email`   | 读             | 对邮件进行分类打标                                 | 正常       |
| `summarize_email`  | 读             | 生成邮件摘要                                       | 正常       |
| `draft_reply`      | **写（内部）** | 生成回复草稿，存入草稿箱                           | 需确认     |
| `send_reply`       | **写（外部）** | 将人工确认过的草稿发送                             | **高风险** |

### 3.2 关键工具Schema示例

```
# fetch_emails
参数:
  folder: 收件箱/已发送/自定义文件夹（默认：收件箱）
  status: 未读/全部（默认：未读）
  limit: 拉取数量（默认：20，最大：50）
  from: 发件人筛选（可选）
  date_from: 起始日期（可选）

返回:
  emails: [{id, subject, from, received_at, has_attachment, snippet}]
  total_count: 符合条件的邮件总数

# draft_reply
参数:
  email_id: 原邮件ID
  tone: 正式/半正式/亲切（默认：匹配总经理风格）
  key_points: 回复要点（用户可指定，可选）
  cc_original_recipients: 是否抄送原件人（默认：false）

返回:
  draft_id: 草稿ID
  subject: 回复标题
  body: 回复正文
  preview: 前100字预览
```



------

## 4. 邮件分类体系

### 4.1 紧急度分级

| 等级         | 定义                     | 期望处理时间 | 示例                                       |
| :----------- | :----------------------- | :----------- | :----------------------------------------- |
| 🔴 **紧急**   | 影响业务运行，需立即响应 | <30分钟      | 客户投诉升级、服务器宕机告警、法务紧急通知 |
| 🟡 **重要**   | 需要本人处理，但可延后   | <4小时       | 合同审批、战略合作洽谈、直接下属汇报       |
| 🟢 **普通**   | 需要知晓或可转交         | <24小时      | 周报、会议邀请、一般询问                   |
| ⚪ **可忽略** | 无需处理                 | 无需回应     | 系统通知、广告、已处理完成的线程           |

### 4.2 动作分类

| 动作       | 说明                   |
| :--------- | :--------------------- |
| `需要回复` | 发件人明确期望答复     |
| `仅需阅读` | 信息同步，无需回复     |
| `可转交`   | 建议转发给某位下属处理 |
| `可归档`   | 已无后续价值           |

**组合示例**：`[紧急] [需要回复] [客户-X项目]`

------

## 5. 回复风格学习与草拟规范

### 5.1 风格学习维度

- **语气**：正式程度（如“请查收” vs “你看看这个”）
- **篇幅**：一句话回复还是分点论述
- **开头/结尾习惯**：如“辛苦了”、“随时沟通”
- **抄送偏好**：是否习惯抄送相关人员
- **中英文混用**：是否经常使用英文术语

### 5.2 学习方式（推荐：范文检索 + 风格画像，微调为备选）

**首选：范文检索（few-shot / RAG）**

- 分析总经理过去3–6个月的“已发送”邮件（需授权）
- 清洗并按段落切分后向量化，存入「风格知识库」
- 草拟回复时，检索与当前邮件语义最相近的3–5段历史回复片段，作为示例注入Prompt
- 每份草稿可展示「参考了哪封历史回复」，可解释、易回滚、冷启动快

**辅助：风格画像（Rubric）**

- 从历史邮件统计抽取语气、篇幅、开头/结尾习惯、抄送偏好、中英文混用等维度特征
- 生成一段风格描述（Style Guide），作为系统提示约束生成

**备选/进阶：微调（LoRA）**

- 当检索效果不足时，用脱敏后的历史数据对基础模型做轻量微调
- 需建立独立评测集，防止过拟合到个别表达习惯

**反馈闭环**

- 草稿被编辑后，用句子级Diff定位被改动的片段
- 统计改动方向（措辞更正式/更口语、篇幅增删、抄送增删）
- 高频改动点沉淀为风格规则；被用户接受保留的草稿回流进入「风格知识库」

### 5.3 回复草稿规范

- 草稿存入邮箱草稿箱（而非直接发送）
- 每份草稿附带：
  - **信心度**：风格匹配度评分（1-10）
  - **关键决策点**：如“此处需确认金额/日期”
  - **一键编辑入口**：可直接修改后确认发送

------

## 6. 安全与合规

### 6.1 安全红线（不可妥协）

- ❌ AI **禁止**调用任何邮件发送API，除非草稿经人工确认
- ❌ AI 不能修改邮箱的“已发送”记录或删除任何邮件
- ❌ 禁止将邮件内容用于模型训练（除非本地部署脱敏后）
- ❌ 禁止在日志中记录邮件正文（仅记录ID和元数据）

### 6.2 确认机制

- 所有`draft_reply`的输出进入“待确认草稿队列”
- 用户界面上明确展示：
  - 📧 原邮件摘要
  - ✍️ AI草拟的回复
  - ✅ [确认发送] [编辑后发送] [放弃]
- 高风险场景（如包含金额、合同条款）**强制二次确认**

### 6.3 审计日志

- 记录所有工具调用：时间、操作类型、操作对象（仅ID）、是否成功
- 记录所有“确认发送”操作：操作人、时间、邮件ID
- 日志保留期≥6个月

### 6.4 安全网关规格（架构图中的「安全网关模块」）

**定位**：位于 Agent 与外部世界的唯一边界，所有外发内容（发送API）必须经网关放行；发送凭据仅由网关持有，Agent/LLM 层接触不到。

**放行矩阵**

| 操作类型 | 默认策略 | 条件 |
| :--- | :--- | :--- |
| 读操作（fetch / search / get_detail / classify / summarize） | ✅ 放行 | 无 |
| 内部写（draft_reply 存入草稿箱） | ✅ 放行 | 生成 draft_id 并进入「待确认队列」 |
| 外部写（send_reply 发送） | ⛔ 默认拒绝 | 须同时满足以下全部前置条件 |

**发送放行的前置条件（全部满足才放行）**

1. 目标草稿存在于「待确认队列」且状态 = 已确认
2. 存在操作人的确认记录（操作人、时间、原邮件ID）
3. 高风险场景已完成二次确认
4. 发送内容与已确认草稿**逐字一致**（内容哈希比对）；草稿一旦被改动，必须重新确认

**高风险场景判定（触发强制二次确认）**

- 正文包含金额、日期承诺、期限承诺
- 涉及合同/法律/商务承诺性用语
- 发件方为外部客户、法务、监管机构

**技术保障**

- 发送API凭据仅由网关持有，Prompt与工具定义中不暴露
- 即使LLM被注入攻击诱导，也无调用发送的能力（无凭据 + send仅在网关层注册）
- 默认会话为只读，只有用户显式进入「发送模式」才激活发送通道
- 一切被网关拒绝的发送尝试：记录告警并进入审计日志

------

## 7. 交互设计

### 7.1 用户意图Router

| 用户输入                             | 识别意图   | 触发流程                           |
| :----------------------------------- | :--------- | :--------------------------------- |
| “帮我看看收件箱” / “今天有什么邮件”  | 每日简报   | fetch→classify→summarize→生成简报  |
| “找一下王总上周发的合同邮件”         | 邮件搜索   | search→get_detail→summarize        |
| “给这封邮件回个信，告诉他下周二可以” | 指定回复   | get_detail→draft_reply→展示草稿    |
| “这封邮件转给张经理处理”             | 转交建议   | 生成转交建议文本                   |
| “最近王总的邮件都说了什么”           | 发件人聚合 | search by from→aggregate→summarize |

### 7.2 每日简报格式（示例）

```
📊 今日邮件简报 | 2026年8月6日 08:30
收件箱共 47 封未读邮件

🔴 紧急（3封）
• [15:20 发] 李总 - X项目合同条款变更，需今天确认
• [09:15 发] 运维 - 线上支付服务异常告警
• [08:02 发] 王律师 - 商标异议答辩截止明日

🟡 重要（9封）
• [昨17:30] 张经理 - Q3预算调整申请
• [昨16:12] A客户 - 续约价格协商
...（可展开）

💡 建议优先处理前2封紧急邮件
```



------

## 8. 开发阶段划分

### Phase 1：最小闭环

- 对接1个邮箱（IMAP）
- 实现邮件拉取 + 分类（规则 + LLM）
- 简易Web界面展示结果
- **不含草拟、不含发送**

### Phase 2：摘要与草拟

- 集成摘要引擎
- 实现回复草拟（基础版，不含风格学习）
- 草稿确认→发送链路打通
- 安全机制就位

### Phase 3：风格学习与优化

- 分析历史已发送邮件，提取风格特征
- 风格匹配度评估
- 用户反馈闭环（草稿修正→模型改进）

### Phase 4：多邮箱与团队协作

- 支持多邮箱接入
- 转交建议生成
- 助理协作模式（助理可查看/修改草稿后提交总经理确认）

------

## 9. 成功标准

| 指标                   | 目标值             | 测量方式       |
| :--------------------- | :----------------- | :------------- |
| 紧急邮件识别准确率     | ≥95%               | 人工标注测试集 |
| 分类综合准确率         | ≥90%               | 每日抽查20封   |
| 摘要可用率（无需重写） | ≥80%               | 用户评分≥4/5   |
| 回复草稿采纳率         | ≥60%（首次即发送） | 草稿发送率统计 |
| 总经理日均邮件处理时间 | 减少≥60%           | 使用前后对比   |
| 单封邮件处理成本       | <¥0.1（含LLM调用） | Token统计      |
| 月安全事故次数         | 0                  | 审计日志监控   |